# Typescript Test

https://www.typescriptlang.org/docs/handbook/typescript-tooling-in-5-minutes.html

`tsc greeter.ts`

```
One note: When you run tsc greeter.ts (specifying a file directly), TypeScript ignores the tsconfig.json. To use your tsconfig, just run tsc without specifying files — it will automatically use tsconfig.json and compile all .ts files in the project.
```

`tsc --showConfig`
